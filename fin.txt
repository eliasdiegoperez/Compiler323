function sum$ [a:int, b:int ] {
    return a + b;
}
function swap [value:boolean] {
    if ( value < true ) return false;
    else return true; endif
}
%%
int a;
get (a);

if (x < y) 
    return x;
endif
